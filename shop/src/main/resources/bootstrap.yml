spring:
  application:
    name: shop
  cloud:
    config:
      label: feature01
      uri: http://localhost:8988
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:file:~/shop
  jpa:
    hibernate:
      ddl-auto: create-drop